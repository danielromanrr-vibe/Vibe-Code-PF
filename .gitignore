# Dependencies
node_modules/

# Build
dist/
*.local
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.log
.vscode/
.idea/

# Vite
*.local

# Local dev runtime state
.cursor/vite-dev.pid

# Source video masters — hundreds of MB each, and the two large ones stream from
# Vimeo rather than this repo. Working copies live under public/.
source-assets/**/*.mp4

# Local scratch / media (do not commit)
.tmp_pydeps/
.tmp-about-verify/
.tmp-thinking-verify/
.tmp-hero-type-verify/
tmp_video_frames/
tmp_video_frames_peaks/
reference-motion.mov
