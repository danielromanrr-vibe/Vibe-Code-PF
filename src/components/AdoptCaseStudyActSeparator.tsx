/** Visual break between major case-study acts — consistent gap + hairline rule. */
export default function AdoptCaseStudyActSeparator() {
  return <div className="adopt-case-study-act-separator" role="separator" aria-hidden />;
}
